spring:
  ai:
    mcp:
      server:
        protocol: STREAMABLE
server:
  port: 8080


# Sequential Thinking Configuration
app:
  thinking:
    session:
      timeout-minutes: 30
      cleanup-interval-minutes: 5
    thought:
      max-per-session: 1000
    branch:
      max-per-session: 100

